# @package _global_

defaults:
  - base
  - _self_

run_name: sd15-inversion-lora-r16-bs128-e3
checkpoint_dir: checkpoints/sd15_inversion_lora_r16_bs128_e3

# 30,441 trajectories * 50 transitions at batch 128: 11,892 steps/epoch.
max_train_steps: 35676
gradient_accumulation_steps: 1

# Precision is a model property, not a separate experiment config.
model:
  torch_dtype: float32
  variant: null

